.box#replies
  = render :partial => "replies/replies_status", :locals => { subject: subject }
  - if subject.replies.size == 0
    #no-reply
      = itext("reply.no_replies")
  - subject.replies.each do |reply|
    = render :partial => "replies/reply", :locals => { :reply => reply }